<h2>ProductionCompanies</h2>
<ol>
  <% @production_companies.each do |company| %>
    <li><%= link_to company.name, company %></li>
    <em>(<%= pluralize company.movies.count, "Top Movies" %>)</em>
  <% end %>
</ol>
